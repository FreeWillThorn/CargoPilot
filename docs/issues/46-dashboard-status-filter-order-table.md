> *This was generated by AI during triage.*

# Issue 46: Dashboard Status Filter Keeps Order Details

## Context Pack

- `docs/prd.md`
- `docs/modules/dashboard.md`
- `docs/modules/user-interface.md`
- `docs/issues/28-dashboard-localization-and-drilldowns.md`
- `docs/issues/40-auto-submit-filters.md`

## Category

Bug.

## State

ready-for-agent.

## Verification

- Confirmed Dashboard has a status dropdown that auto-submits.
- Confirmed Dashboard's bottom order table is still labeled `进口订单`.
- Reporter says selecting an order status makes the order detail/table area disappear or stop behaving as the order-detail area.
- Searched existing issues and docs for Dashboard order-detail/table behavior; no implemented issue covers this exact filtered-state regression and label change.
- No matching `.out-of-scope/` rejection exists.

## Agent Brief

**Category:** bug

**Summary:** Dashboard status filtering should keep the order detail/list visible and rename the bottom table to 出口订单.

**Current behavior:**

Dashboard includes a status filter and bottom order table. After selecting an order status, the reporter sees the order detail/list area missing or no longer useful. The table heading is still `进口订单`.

**Desired behavior:**

Changing the Dashboard order-status filter should refresh Dashboard but keep the bottom order table/details visible. If the filter returns matching orders, those orders should be listed with their clickable drilldowns intact. If no orders match, the same table area should remain visible with an empty-state row. The bottom section heading should be `出口订单`.

**Key interfaces:**

- Dashboard status filter and order-card/table rendering.
- Order-status label helpers should continue to show Chinese labels.
- Drilldown links from Dashboard rows/cards should continue to open the relevant workflow section with context preserved.

**Acceptance criteria:**

- [ ] Selecting any Dashboard order status keeps the bottom order table/details section rendered.
- [ ] Empty filtered results show a visible empty state instead of removing the section.
- [ ] The bottom Dashboard table heading is `出口订单`.
- [ ] Existing Dashboard drilldown links still work after filtering.
- [ ] Tests cover Dashboard with a status filter and the new section label.

**Out of scope:**

- Dashboard visual redesign.
- New charts or analytics.
- Changing order status semantics.
