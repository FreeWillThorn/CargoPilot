> *This was generated by AI during planning.*

# Issue 74: 订单智能体 Section Shell And Retained Conversation

## Category

Enhancement.

## State

ready-for-agent.

## Context Pack

- `CONTEXT.md`
- `docs/prd-order-agent.md`
- `docs/modules/order-agent.md`
- `docs/adr/0005-order-agent-can-start-without-order.md`
- `docs/adr/0006-order-agent-uses-retained-conversations.md`
- `docs/adr/0007-order-agent-requires-live-model.md`
- `cargopilot/web.py`
- `tests/test_web.py`

## Summary

Add the first demoable `订单智能体` Workflow Section with retained conversations, an optional Import Order selector, and a business-workbench layout.

## Requirements

- Add `订单智能体` as a first-level navigation item above `AI资料收集箱`.
- Keep `AI资料收集箱` available as the fallback demo path.
- Provide a retained conversation list and current-conversation workspace.
- Support creating, opening, and closing a conversation without running any model call yet.
- Include an optional Import Order selector, upload control placeholder, natural-language input, empty Agent Processing Trace, and empty result area.
- Use scroll containers for the conversation list and workspace so the page remains usable at real data volume.

## Acceptance Criteria

- [ ] Admin Users can open `订单智能体` from navigation.
- [ ] A new conversation can be created without selecting an Import Order.
- [ ] A conversation can optionally be associated with a selected Import Order.
- [ ] Refreshing the page preserves the conversation list and current conversation state.
- [ ] `AI资料收集箱` still appears and still works as a separate section.
- [ ] Relevant web tests cover navigation, empty state, retained conversation, and scroll-container classes.

## Blocked by

None - can start immediately.
