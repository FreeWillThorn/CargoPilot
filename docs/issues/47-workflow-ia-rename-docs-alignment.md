> *This was generated by AI during triage.*

# Issue 47: Workflow IA Rename And Docs Alignment

## Context Pack

- `docs/adr/0001-import-order-as-system-center.md`
- `docs/adr/0002-order-workflow-navigation.md`
- `docs/modules/user-interface.md`
- `docs/modules/orders-goods.md`
- `docs/modules/dashboard.md`
- `docs/issues/20-section-navigation-shell.md`
- `docs/issues/21-order-project-section.md`
- `docs/issues/22-goods-tracking-section.md`

## Category

Bug.

## State

ready-for-agent.

## Verification

- Confirmed current docs and navigation still use `订单项目` and `货物跟踪`.
- Confirmed current docs say Goods Lines appear inside both 订单项目 and 货物跟踪.
- Reporter has now clarified the intended workflow split:
  - `订单项目` should become `订单详情`.
  - `货物跟踪` should become `货物详情`.
  - Goods Line detail/editing should live in 货物详情, not as a parallel/duplicated block in 订单详情.
- No matching `.out-of-scope/` rejection exists.

## Agent Brief

**Category:** bug

**Summary:** Update the product/navigation contract so future development uses 订单详情 and 货物详情 with clear ownership boundaries.

**Current behavior:**

The docs and navigation contract still describe the old section names and responsibilities. 订单项目 owns Import Orders plus Goods Lines; 货物跟踪 focuses on logistics status and exceptions. This conflicts with the reporter's clarified workflow model.

**Desired behavior:**

Docs should define the primary left navigation as:

- Dashboard
- 订单详情
- 货物详情
- 仓库盘点
- 海运单证
- 成本利润

Section ownership should be updated:

- 订单详情 owns Export/Import Order list, selected order details, and order CRUD.
- 订单详情 should not own the Goods Line detail/table module after the migration.
- 货物详情 owns Goods Line list/detail/editing, logistics status updates, product fields, supplier fields, Excel goods import, and Goods Line delete.
- 仓库盘点 owns Warehouse inventory plus Warehouse master-data CRUD.
- 收货客户 master-data CRUD belongs inside 订单详情.
- Top-right 管理/设置 should no longer be the primary place for 收货客户 or 仓库资料 after those modules are migrated.

**Key interfaces:**

- Navigation labels and active-route mapping.
- User-interface module docs and ADR describing workflow sections.
- Existing issue context packs that reference the old section names should be updated only where necessary to prevent future implementation conflicts.

**Acceptance criteria:**

- [ ] Navigation/docs consistently use `订单详情` and `货物详情`.
- [ ] Docs clearly state Goods Line detail/editing belongs to 货物详情.
- [ ] Docs clearly state 收货客户 CRUD belongs to 订单详情.
- [ ] Docs clearly state 仓库资料 CRUD belongs to 仓库盘点.
- [ ] Existing implementation issues that future agents are likely to read do not contradict the updated section ownership.
- [ ] No runtime behavior change is required in this issue unless a label-only change is trivial and low risk.

**Out of scope:**

- Full UI migration of the sections.
- Removing database tables or changing domain model names.
- Changing backend route names if visible labels can be corrected without it.
