# Issue 30: Tracking Localization and Responsive Table

## Context Pack

- `docs/prd.md`
- `docs/modules/user-interface.md`
- `docs/modules/dashboard.md`
- `docs/issues/22-goods-tracking-section.md`

## Category

Bug.

## Goal

货物详情 should show Chinese logistics statuses and fit smaller browser widths without breaking the page.

## Scope

- Translate Goods Logistics Status values in filters and rows.
- Keep submitted form values as internal enum values.
- Add a responsive wrapper for the tracking table so wide columns scroll horizontally and long order lists scroll vertically inside the table panel.
- Keep the table panel height inside the first viewport on normal desktop screens; the horizontal scrollbar should not sit below the visible viewport.
- Use the same status-label helper where Dashboard/order pages also display Goods Logistics Status if touched.

## Acceptance Criteria

- Users see labels such as 未下单, 已下单, 国内运输中, 已入仓, 异常.
- Status filters still submit the correct internal value.
- The 货物详情 table remains usable on narrow widths via horizontal scrolling.
- The 货物详情 table remains usable with many Goods Lines via vertical scrolling inside the panel.
- The 货物详情 table shows package/weight/volume summary columns: 每箱数量, 外箱尺寸(cm), 单箱毛重(kg), CBM, 总毛重(kg).
- Tests cover one Chinese logistics status label.

## Triage Revision Notes

> *This was generated by AI during triage.*

- 2026-06-26: Confirmed the implementation uses `tracking-scroll` for the 货物详情 table, with both horizontal and vertical overflow handled inside the table panel.
- 2026-06-26: Adjusted `tracking-scroll` height to be viewport-based, so the internal horizontal scrollbar remains visible without needing to scroll the whole page first.

## Out of Scope

- Carrier API integration.
- Mobile-native layout.
